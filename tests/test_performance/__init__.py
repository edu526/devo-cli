"""Performance tests for Devo CLI.

This package contains performance and benchmark tests to ensure the CLI
maintains acceptable performance characteristics and to detect regressions.

Tests are marked with @pytest.mark.slow and are skipped by default in
local development. They run in CI to catch performance regressions.
"""
