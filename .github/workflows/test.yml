name: Tests

on:
  pull_request:
    branches:
      - main
      - develop
  push:
    branches:
      - main
      - develop

jobs:
  test:
    name: Run Tests
    uses: ./.github/workflows/test-reusable.yml
    with:
      python-version: '3.12'
